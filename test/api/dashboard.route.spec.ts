import { describe, it, expect, vi, beforeEach } from 'vitest'

// Minimal API test placeholder — keep lightweight for now
describe('dashboard API (minimal)', () => {
  it('sanity', () => {
    expect(true).toBe(true)
  })
})

